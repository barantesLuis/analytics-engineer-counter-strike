name: 'cs_analytics'
version: '1.0.0'
config-version: 2

profile: 'cs_analytics'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
analysis-paths: ["analyses"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  cs_analytics:
    bronze:
      +materialized: view

    silver:
      +materialized: view

    gold:
      +materialized: table